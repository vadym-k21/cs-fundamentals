1. Big O - calculate worst time and space complexity for an algo or DS based on input
2. Consider worst case scenario, even if there are possible faster variations
3. Calculate amount of loops and constants (O(2N) = O(N))
4. Most important in speed:
    1. O(1)
    2. O(logn) - split the array in hal on every step
    3. O(n) - x of input equals x of loops
    4. O(nlogn) - split array in hlaf and scan the value (if it is an raay or string)
    5. O(Nˆ2) - real max of actual systems - 2 loops per one input 
    6. O(2ˆN)
    7. O(n!)
